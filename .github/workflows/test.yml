name: Test workflow

on:
  push:
    branches:
      - main

jobs:
  checkout-and-use-yaml:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout del repositorio actual
        uses: actions/checkout@v3

      - name: Ejecutar pipeline template
        uses: 'vazqueztomas/pipelines-template/python/action.yml@main'
          