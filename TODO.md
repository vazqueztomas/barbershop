# Database
add connection to database (noSql) DONE
    -add testing to connect to database 
    -create user (admin)
    -save csv in Database DONE

# Improve UI
add styles to button
add styles to forms
reacommodate all labels and buttons

# Improve logic
Improve logic for buttons in haircuts tables
Use pydantic when possible
SPIKE - Find a solution to "tabs" in Tkinter
Add filters (with correct logic)

# Add IA - Graphics
SPIKE Check if we can add IA for improvements
Add graphics using matplotlib (in a new tab)

# Add linters and format configurations
Add Ruff - "DONE"
Add pylint
Add pre-commit configs - "DONE"
Investigate about UV - "DONE"

# Investigate about MP for cashing
Investigate MercadoPago to receive money
